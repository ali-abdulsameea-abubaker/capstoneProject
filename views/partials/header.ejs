<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title || 'Pet Care Management' %></title>
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Bootstrap Icons -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <!-- Custom CSS -->
  <link rel="stylesheet" href="/style.css">
  <link rel="stylesheet" href="/css/theme.css">
  <style>
    /* Enhanced header styles */
    body {
      font-family: 'Poppins', sans-serif;
    }
    
    .navbar-custom {
      background: linear-gradient(135deg, #4CAF50 0%, #45a049 100%);
      box-shadow: 0 2px 15px rgba(0, 0, 0, 0.1);
      padding: 0.5rem 0;
    }
    
    .navbar-brand {
      font-weight: 700;
      font-size: 1.4rem;
      color: white !important;
      display: flex;
      align-items: center;
      margin-right: 1rem;
    }
    
    .navbar-brand i {
      font-size: 1.6rem;
      margin-right: 0.5rem;
    }
    
    .nav-link {
      font-weight: 500;
      color: rgba(255, 255, 255, 0.9) !important;
      padding: 0.5rem 0.8rem !important;
      border-radius: 18px;
      margin: 0 0.1rem;
      transition: all 0.3s ease;
      font-size: 0.95rem;
    }
    
    .nav-link:hover {
      background: rgba(255, 255, 255, 0.15);
      color: white !important;
      transform: translateY(-1px);
    }
    
    .navbar-text {
      color: white !important;
      font-weight: 500;
      font-size: 0.9rem;
    }
    
    .btn-outline-light {
      border: 1.5px solid rgba(255, 255, 255, 0.7);
      color: white;
      border-radius: 18px;
      font-weight: 500;
      font-size: 0.9rem;
      padding: 0.4rem 0.8rem;
      transition: all 0.3s ease;
    }
    
    .btn-outline-light:hover {
      background: white;
      color: #4CAF50;
      transform: translateY(-1px);
    }
    
    .btn-outline-danger {
      border: 1.5px solid rgba(255, 255, 255, 0.7);
      color: white;
      border-radius: 18px;
      font-weight: 500;
      font-size: 0.9rem;
      padding: 0.4rem 0.8rem;
      transition: all 0.3s ease;
    }
    
    .btn-outline-danger:hover {
      background: #dc3545;
      color: white;
      border-color: #dc3545;
      transform: translateY(-1px);
    }
    
    .back-to-home {
      background: rgba(255, 255, 255, 0.1);
      color: white;
      border: none;
      border-radius: 18px;
      padding: 0.3rem 0.8rem;
      font-weight: 500;
      font-size: 0.85rem;
      transition: all 0.3s ease;
      display: inline-flex;
      align-items: center;
      text-decoration: none;
    }
    
    .back-to-home:hover {
      background: rgba(255, 255, 255, 0.2);
      color: white;
      transform: translateY(-1px);
    }
    
    .navbar-toggler {
      border: 1.5px solid rgba(255, 255, 255, 0.7);
      color: white;
      padding: 0.25rem 0.5rem;
    }
    
    .navbar-toggler:focus {
      box-shadow: 0 0 0 2px rgba(255, 255, 255, 0.25);
    }
    
    /* Theme toggle button */
    .theme-toggle {
      width: 38px;
      height: 38px;
      padding: 0;
      display: flex;
      align-items: center;
      justify-content: center;
      border-radius: 50%;
    }
    
    /* User profile section */
    .user-profile {
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }
    
    .user-avatar {
      width: 32px;
      height: 32px;
      object-fit: cover;
      border: 2px solid rgba(255, 255, 255, 0.3);
    }
    
    /* Mobile responsiveness */
    @media (max-width: 991.98px) {
      .navbar-nav {
        margin: 0.5rem 0;
      }
      
      .nav-link {
        margin: 0.2rem 0;
        text-align: center;
        padding: 0.6rem 1rem !important;
      }
      
      .navbar-nav .btn {
        margin: 0.3rem 0;
        width: 100%;
        text-align: center;
        justify-content: center;
      }
      
      .navbar-text {
        text-align: center;
        margin: 0.5rem 0;
      }
      
      .back-to-home {
        margin: 0.5rem auto;
        display: flex;
        justify-content: center;
        max-width: 160px;
      }
      
      .user-profile {
        justify-content: center;
        margin: 0.5rem 0;
      }
      
      .theme-toggle {
        margin: 0.3rem auto;
        display: flex;
        justify-content: center;
      }
      
      /* Stack navbar items vertically on mobile */
      .navbar-nav .nav-item {
        width: 100%;
      }
      
      .navbar-nav .nav-item .nav-link,
      .navbar-nav .nav-item form {
        width: 100%;
      }
    }
    
    /* Desktop specific styles */
    @media (min-width: 992px) {
      .navbar-nav .nav-item {
        display: flex;
        align-items: center;
      }
      
      /* Right align user controls on desktop */
      .navbar-nav.ms-auto {
        margin-left: auto !important;
      }
    }
    
    /* Animation for better visual appeal */
    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(-8px); }
      to { opacity: 1; transform: translateY(0); }
    }
    
    .navbar-nav .nav-item {
      animation: fadeIn 0.4s ease forwards;
    }
  </style>
</head>
<body>
  <!-- Navigation -->
  <nav class="navbar navbar-expand-lg navbar-custom">
    <div class="container container-custom">
      <a class="navbar-brand" href="/">
        <i class="bi bi-heart-pulse"></i><strong>Pet Care</strong>
      </a>

      <!-- Back to Home Button - Only show when not on landing page -->
      <!-- <% if (typeof showBackButton === 'undefined' || showBackButton) { %>
        <a href="/" class="back-to-home d-none d-lg-flex">
          <i class="bi bi-arrow-left me-1"></i>Home
        </a>
      <% } %> -->
    
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
        <span class="navbar-toggler-icon"></span>
      </button>
      
      <div class="collapse navbar-collapse" id="navbarNav">
        <!-- Main navigation -->
        <ul class="navbar-nav me-auto">
          <% if (typeof username !== 'undefined') { %>
            <li class="nav-item">
              <a class="nav-link" href="/dashboard">
                <i class="bi bi-speedometer2 me-1"></i>Dashboard
              </a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/pets/add">
                <i class="bi bi-heart me-1"></i>Pets
              </a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/schedule-task">
                <i class="bi bi-calendar-check me-1"></i>Tasks
              </a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/health">
                <i class="bi bi-clipboard-pulse me-1"></i>Health
              </a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/community">
                <i class="bi bi-people me-1"></i>Community
              </a>
            </li>
          <% } %>
        </ul>

        <!-- User controls -->
        <ul class="navbar-nav ms-auto">
          <% if (typeof username !== 'undefined') { %>
            
            
            <!-- User profile (desktop) -->
            <li class="nav-item d-none d-md-block">
              <div class="user-profile">
                <a class="nav-link" href="/profile"><img 
                  src="<%= typeof profilePicture !== 'undefined' ? profilePicture : '/images/default-avatar.png' %>" 
                  class="user-avatar rounded-circle" 
                  onerror="this.src='/images/default-avatar.png'"
                  alt="Profile"
                ></a>
                <span class="navbar-text d-none d-lg-inline"> welcome 
                  <%= username %>
                </span>
              </div>
            </li>
            
            <!-- Profile link
            <li class="nav-item">
              <a class="nav-link" href="/profile">
                <i class="bi bi-person-circle me-1 d-none d-md-inline"></i>
                <span class="d-md-none">Profile</span>
              </a>
            </li> -->

            <!-- add small space-->

&nbsp&nbsp
            
            <!-- Logout button -->
            <li class="nav-item">
              <form action="/auth/logout" method="POST" class="d-flex">
                <button type="submit" class="btn btn-outline-danger">
                  <i class="bi bi-box-arrow-right me-1"></i>
                  <span class="d-none d-md-inline">Logout</span>
                </button>
              </form>
            </li>

&nbsp&nbsp
&nbsp&nbsp


            <!-- Theme Toggle Button -->
            <li class="nav-item d-none d-md-block">
              <button id="themeToggle" class="btn btn-outline-light theme-toggle" title="Toggle dark/light mode">
                <i class="bi bi-sun-fill d-none" id="lightIcon"></i>
                <i class="bi bi-moon-fill" id="darkIcon"></i>
              </button>
            </li>
            
          <% } else { %>
            <!-- Login and Register buttons for non-authenticated users -->
            <li class="nav-item">
              <a class="btn btn-outline-light me-2" href="/login">
                <i class="bi bi-box-arrow-in-right me-1"></i>Login
              </a>
            </li>
            <li class="nav-item">
              <a class="btn btn-light" href="/register">
                <i class="bi bi-person-plus me-1"></i>Register
              </a>
            </li>
          <% } %>
        </ul>

        <!-- Show back button on mobile -->
        <% if (typeof showBackButton === 'undefined' || showBackButton) { %>
          <div class="d-lg-none text-center mt-2 pt-2 border-top">
            <a href="/" class="back-to-home">
              <i class="bi bi-arrow-left me-1"></i>Back to Home
            </a>
          </div>
        <% } %>
      </div>
    </div>
  </nav>

  <main class="flex-grow-1">
    <div class="container-custom py-4">